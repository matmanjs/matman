{
  "name": "mockstar-app",
  "scripts": {
    "start": "mockstar start --watch",
    "dev": "mockstar run --dev --watch",
    "stop": "mockstar stop"
  },
  "dependencies": {
    "mockstar": "^1.1.4"
  }
}
