{
  "name": "baidu_02",
  "version": "1.0.0",
  "scripts": {
    "build": "matman build",
    "build-dev": "matman build --dev",
    "test": "npm run build && mocha",
    "test:show": "cross-env SHOW_BROWSER=1 npm run test"
  },
  "dependencies": {
    "matman": "^6.0.2",
    "matman-crawler": "^6.0.2",
    "matman-runner-puppeteer": "^6.0.2"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "cross-env": "^7.0.2",
    "matman-cli": "^6.0.2",
    "mocha": "^7.1.2"
  }
}
